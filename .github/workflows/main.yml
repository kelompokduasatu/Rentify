name: Temporary Action For Rentify

on: [push]

jobs:
  action-test:
    runs-on: ubuntu-latest 
    
    steps:
      - name: Mencetak pesan sapaan
        run: echo "Hello."
        
      - name: Menampilkan tanggal dan waktu server
        run: date